apiVersion: v1
kind: Service
metadata:
  name: registry
spec:
  type: NodePort

  selector:
    app: registry

  ports:
    - name: registry
      port: 5000
      targetPort: registry
      nodePort: 30500
      protocol: TCP
